<div class="center jumbotron">


   <h1>The Gossip Project</h1>
   <br>
   <p>
    Titre : <%= Gossip.find(@id).title%>
    <br>
    Potin: <%= Gossip.find(@id).content%>
    <br>
    Date de création :  <%=Gossip.find(@id).created_at%>
   </p>


     <%= link_to "Editer", edit_gossip_path %>
<br> <br>
     <%= link_to 'Supprimer', @gossip, method: :delete %>
<br><br>
     <%= link_to "Index", '#' %>


 <br> <br>
<h4> <%= @gossip.comments.count %> Commentaires</h4>

<% Comment.all do |comment| %>
  <% if comment.gossip_id = @id %>
    <p>
      <div class="card" style="width: 18rem;">
      <h5 class="card-title"><%=comment.user.username%></h5>
      <h6 class="card-subtitle mb-2 text-muted"><%=gossip.content%></h6>
      <%=gossip.content%>
    </div>
    </p>
    <% end %>
  <% end %>


<br><br>
<h5> Quelque chose à dire ? </h5>
<br>
<%= render 'comment/form' %>
